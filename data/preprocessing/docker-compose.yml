version: "3"

services:
  fpena_preprocessing:
    image: fpena_preprocessing
    build: .
    restart: "no"
    command: npm run start
    volumes:
      - h36m:/data/h36m

volumes:
  h36m:
    external: true
